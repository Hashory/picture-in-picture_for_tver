## 1. プロジェクトの目的とスコープ

- この拡張機能は **TVer (`tver.jp`) で Picture-in-Picture を使えるようにする**
  ことが目的です。
- 公式機能ではないため、挙動変更やUI変更は慎重に行ってください。
- 要件に明示がない限り、以下はスコープ外です。
  - 他サイト対応
  - 解析/トラッキング機能追加
  - 権限拡大
  - 大規模なUI再設計

## 2. リポジトリ構成と責務

- `source/contentscript.ts`
  - TVerページ内でのDOM監視、PinPボタン挿入、ショートカット処理の本体。
- `source/background.ts`
  - バックグラウンド(service worker)。現在はアンインストールURL設定のみ。
- `source/pinp_icon.ts`
  - PinPアイコンデータ。
- `public/manifest.json`
  - Manifest V3設定。
- `public/popup.html`
  - 拡張ポップアップUI。
- `bundle.ts`
  - Denoで`source/*.ts`をバンドルし`dist`へ出力、`public`をコピー。
- `dist/`
  - 生成物。通常は手編集しない。

## 3. 開発コマンド

作業前後で以下を実行してください。

```sh
deno cache deno.json
deno task bundle
deno fmt
deno lint
deno check source/*.ts bundle.ts
```

- エージェントは、変更対象に無関係な大規模修正を避けてください。
- 整形のみの差分が大量に出る場合は、変更範囲を再検討してください。

## 4. 実装ルール

- 既存のコードスタイルを維持する。
  - インデントはタブ。
  - 文字列はシングルクオート。
- 1文字変数や過剰な抽象化を避け、既存の簡潔な実装方針に合わせる。
- 追加依存は原則避ける。必要な場合は理由をPR/説明に明記する。
- 既存コメント・文言は日本語中心だが、英語追加も可（周辺トーンと整合させる）。

## 5. Manifest / アーキテクチャ制約

- 本プロジェクトは Manifest V3。
- `content_scripts.world` は現在 `"MAIN"` を採用。
  - **明示的な要求がない限り変更しないこと。**
- `host_permissions` は最小化を維持し、`*://tver.jp/*`
  以外の追加は要件ベースでのみ実施。
- `contentscript.ts` は TVer
  側DOMクラス名に依存するため、セレクタ変更時は副作用を慎重に確認する。

## 6. セキュリティ / プライバシー方針

- ユーザーデータの収集・送信機能を追加しない（明示要件がある場合を除く）。
- 外部URL（ポップアップリンク、アンインストールURL等）の追加・変更は安全性と妥当性を確認する。
- 権限追加、外部通信追加、`eval`相当の導入は要件と根拠を必須とする。

## 7. 変更時の確認チェックリスト

最低限、TVer上で次を手動確認する。

1. プレイヤーにPinPボタンが表示される。
2. ボタンクリックでPinPに入れる/解除できる。
3. `P` / `p` キーでPinPトグルできる（入力欄フォーカス時は反応しない）。
4. ページ遷移（SPA遷移）後もボタンが再生成される。
5. ミニプレイヤー表示時にUI崩れが致命的でない。
6. エラー時のユーザー通知が不自然でない。

## 8. 変更方針（Do / Don’t）

### Do

- 問題の根本原因に対処する。
- 変更は小さく保ち、影響範囲を明示する。
- 関連ドキュメント（READMEやmanifest versionなど）を必要に応じて更新する。

### Don’t

- 要件にないリファクタリングを広範囲に行う。
- 無関係なファイルをついでに変更する。
- 生成物`dist`の手修正を前提にする。

## 9. リリース運用（ローカル運用）

- このリポジトリでは、まず `deno task bundle` で `dist` を生成する。
- ローカル検証時は `dist` を「パッケージ化されていない拡張機能」として読み込む。
- バージョン更新が必要な変更では、`public/manifest.json` の `version`
  を忘れず更新する。

---

迷う場合は「最小変更」「権限最小化」「TVer専用スコープ維持」を優先してください。
